# 2026-08-13 工作日志

## GitHub 贡献热力图美化
- 问题：用户反馈热力图"还是有点丑"（信息空洞、视觉层次弱）。
- 改动：
  - `build.js`：新增 `computeContributionStats(calendar)`，构建期算出 `activeDays` / `currentStreak` / `longestStreak`，并挂到 `stats.github`。
  - `templates/home.html`：在 `ghcal-card` 顶部新增 4 格概览统计（总贡献、当前连续、最长连续、活跃天数）。
  - `static/css/style.css`：
    - 卡片加蓝色渐变描边高光（`::before` + `-webkit-mask-composite`）。
    - 概览小卡蓝调渐变背景 + hover 浮起光晕。
    - 热力图格子 hover 放大 + 蓝色 `drop-shadow` 发光。
    - 重调 5 级蓝色梯度，提升明暗对比。
    - 图例色块同步新配色；移动端概览改为 2 列。
- 验证：从已部署页面提取真实 SVG（69 次贡献、16 活跃天、当前连续 2、最长连续 3），用新 CSS 渲染截图，亮/暗主题均正常。
- 提交：`9440048` feat: 美化 GitHub 贡献热力图（概览统计+渐变描边+hover 发光+配色优化）

## 通盘检查与交付
- 执行完整交付检查：
  - 构建零错误（`npm run build` exit 0，55 HTML，98 文件）。
  - 资源与链接完整性：全站 55 HTML 共 **0 缺失本地资源、0 坏链**。
  - HTML 结构：0 重复 id、0 缺失 alt、0 未闭合标签、0 缺失 lang。
  - JS 语法：`main.js` / `build.js` 均 `node --check` 通过。
  - 关键外链（nwpu、buaa、GitHub）均 200。
- 修复检查中发现的真实问题：
  - `content/courses/deep-learning/index.md` 与 `content/courses/leetcode_hot100/index.md` 手写章节导航列表与模板统一卡片列表重复，且 deep-learning 中 `lec01-02` 等链接、leetcode 中 `C++基础.html` 链接存在坏链（构建期 slug 化为 `c-基础.html`）。
  - 清理方案：移除两门课程 index.md 中手写且重复的章节导航，完全由模板生成的「课程笔记」卡片列表统一渲染，确保链接经 slugify 正确生成。
- 提交：`337c295` refactor: 移除课程 index.md 中手写且重复/坏链的章节导航（统一由模板课程笔记列表渲染）
- 推送：`git push origin main` **成功**（`1fee3f3..337c295`），GitHub Actions 已触发（run `31624552982` in_progress）。
- 当前状态：本地 main 与 origin/main 一致；等待 Actions 部署完成后，线上课程页将只剩统一、美观、无坏链的笔记卡片列表。

## 教育背景文字配色协调
- 用户反馈 Hero 教育背景两行学校文字颜色突兀。
- 根因：上方"🎓 教育背景"标签用柔和灰 `var(--gray)`，而 `.edu-line` 文字用近黑 `var(--text-heading)`，同块内不一致。
- 修复：`.edu-line` 文字颜色改为 `var(--gray)`（亮 `#6e6e73` / 暗 `#a1a1a6`），与上方标签及其他副标题统一；蓝色圆点保留作点缀。
- 由 auto-commit hook 提交（HEAD a7a8b9b "1"）。

## GitHub 活跃度 Apple Style 重做（第二轮）
- 用户反馈热力图"非常丑"，要求按 Apple Style 精美重设计。
- 改动：
  - `build.js` `buildContributionSvg`：格子放大（cell 11→13，gap 3→4，rx 2.6→3.2），新增「今日」高亮 class `gh-today`（描边）。
  - `static/css/style.css` ghcal 段整体重写：
    - 卡片：毛玻璃 `blur(24px) saturate(180%)` + 分层阴影（内高光 + 投影）+ 柔和蓝紫渐变描边。
    - 概览数字改 SF Pro 大号近黑/白（`var(--text-heading)`、`letter-spacing:-0.02em`、tabular-nums），hover 弹性浮起。
    - 新增内嵌 `.ghcal-canvas` widget 面板（极淡蓝径向底 + 内阴影）。
    - 5 级蓝改为更平滑的 Apple 系统蓝渐变（亮 l4 `#0056d6`、暗 l4 `#4f9bff`）。
    - hover 改弹性 `scale(1.32)` + 柔和蓝光晕（`cubic-bezier(.34,1.56,.64,1)`），图例圆点加大圆角。
  - `templates/home.html`：热力图 SVG 包裹进 `.ghcal-canvas` 容器。
- 验证：`node --check` 通过；用真实 `buildContributionSvg` + mock 53 周日历生成 `github-activity-preview.html`（含亮/暗切换）供预览；`npm run build` exit 0，public 已含新样式。
- 提交：`dd16a0f` style: redesign GitHub activity heatmap in Apple style
- 推送：`git push origin main` 被环境代理对 github.com 的 502 阻断（与历史一致），本地提交已就绪，待网络恢复后推送即触发 CI 部署（真实热力图仅在 CI 带 GH_CONTRIB_TOKEN 时生成）。
- 注意：根目录 `github-activity-preview.html` 为预览用 mock 文件，**勿提交**。
