# 2026-08-13 工作日志

## GitHub 贡献热力图美化
- 问题：用户反馈热力图"还是有点丑"（信息空洞、视觉层次弱）。
- 改动：
  - `build.js`：新增 `computeContributionStats(calendar)`，构建期算出 `activeDays` / `currentStreak` / `longestStreak`，并挂到 `stats.github`。
  - `templates/home.html`：在 `ghcal-card` 顶部新增 4 格概览统计（总贡献、当前连续、最长连续、活跃天数）。
  - `static/css/style.css`：
    - 卡片加蓝色渐变描边高光（`::before` + `-webkit-mask-composite`）。
    - 概览小卡蓝调渐变背景 + hover 浮起光晕。
    - 热力图格子 hover 放大 + 蓝色 `drop-shadow` 发光。
    - 重调 5 级蓝色梯度，提升明暗对比。
    - 图例色块同步新配色；移动端概览改为 2 列。
- 验证：从已部署页面提取真实 SVG（69 次贡献、16 活跃天、当前连续 2、最长连续 3），用新 CSS 渲染截图，亮/暗主题均正常。
- 提交：`9440048` feat: 美化 GitHub 贡献热力图（概览统计+渐变描边+hover 发光+配色优化）

## 通盘检查与交付
- 执行完整交付检查：
  - 构建零错误（`npm run build` exit 0，55 HTML，98 文件）。
  - 资源与链接完整性：全站 55 HTML 共 **0 缺失本地资源、0 坏链**。
  - HTML 结构：0 重复 id、0 缺失 alt、0 未闭合标签、0 缺失 lang。
  - JS 语法：`main.js` / `build.js` 均 `node --check` 通过。
  - 关键外链（nwpu、buaa、GitHub）均 200。
- 修复检查中发现的真实问题：
  - `content/courses/deep-learning/index.md` 与 `content/courses/leetcode_hot100/index.md` 手写章节导航列表与模板统一卡片列表重复，且 deep-learning 中 `lec01-02` 等链接、leetcode 中 `C++基础.html` 链接存在坏链（构建期 slug 化为 `c-基础.html`）。
  - 清理方案：移除两门课程 index.md 中手写且重复的章节导航，完全由模板生成的「课程笔记」卡片列表统一渲染，确保链接经 slugify 正确生成。
- 提交：`337c295` refactor: 移除课程 index.md 中手写且重复/坏链的章节导航（统一由模板课程笔记列表渲染）
- 推送：`git push origin main` **成功**（`1fee3f3..337c295`），GitHub Actions 已触发（run `31624552982` in_progress）。
- 当前状态：本地 main 与 origin/main 一致；等待 Actions 部署完成后，线上课程页将只剩统一、美观、无坏链的笔记卡片列表。

## 构建项目
- 运行 `node build.js`（托管 Node 22.22.2）构建成功，exit 0，输出 `public/`（9.9M）。
- 内容统计：courses 7 门、projects 2 个、blog 6 篇、github repos=3；共渲染 55 HTML + sitemap.xml（54 URL）+ search-index.json（6 posts）。

## 教育背景文字配色协调
- 用户反馈 Hero 教育背景两行学校文字颜色突兀。
- 根因：上方"🎓 教育背景"标签用柔和灰 `var(--gray)`，而 `.edu-line` 文字用近黑 `var(--text-heading)`，同块内不一致。
- 修复：`.edu-line` 文字颜色改为 `var(--gray)`（亮 `#6e6e73` / 暗 `#a1a1a6`），与上方标签及其他副标题统一；蓝色圆点保留作点缀。
- 由 auto-commit hook 提交（HEAD a7a8b9b "1"）。

## GitHub 活跃度 Apple Style 重做（第二轮）
- 用户反馈热力图"非常丑"，要求按 Apple Style 精美重设计。
- 改动：
  - `build.js` `buildContributionSvg`：格子放大（cell 11→13，gap 3→4，rx 2.6→3.2），新增「今日」高亮 class `gh-today`（描边）。
  - `static/css/style.css` ghcal 段整体重写：
    - 卡片：毛玻璃 `blur(24px) saturate(180%)` + 分层阴影（内高光 + 投影）+ 柔和蓝紫渐变描边。
    - 概览数字改 SF Pro 大号近黑/白（`var(--text-heading)`、`letter-spacing:-0.02em`、tabular-nums），hover 弹性浮起。
    - 新增内嵌 `.ghcal-canvas` widget 面板（极淡蓝径向底 + 内阴影）。
    - 5 级蓝改为更平滑的 Apple 系统蓝渐变（亮 l4 `#0056d6`、暗 l4 `#4f9bff`）。
    - hover 改弹性 `scale(1.32)` + 柔和蓝光晕（`cubic-bezier(.34,1.56,.64,1)`），图例圆点加大圆角。
  - `templates/home.html`：热力图 SVG 包裹进 `.ghcal-canvas` 容器。
- 验证：`node --check` 通过；用真实 `buildContributionSvg` + mock 53 周日历生成 `github-activity-preview.html`（含亮/暗切换）供预览；`npm run build` exit 0，public 已含新样式。
- 提交：`dd16a0f` style: redesign GitHub activity heatmap in Apple style
- 推送：`git push origin main` 被环境代理对 github.com 的 502 阻断（与历史一致），本地提交已就绪，待网络恢复后推送即触发 CI 部署（真实热力图仅在 CI 带 GH_CONTRIB_TOKEN 时生成）。
- 注意：根目录 `github-activity-preview.html` 为预览用 mock 文件，**勿提交**。

## ⌘K 全局搜索 + 热力图三配色 + Apple 字体（第三轮）
- 用户需求：①热力图 5 档色阶三套配色 + 再套玻璃卡片；②全站 Apple 字体；③顶栏全局搜索（⌘K/Ctrl+K 唤起、Esc/遮罩关闭、加权搜索标题>标签>描述>分类>正文、实时、空查询最近6篇、结果含分类+阅读时长+标题+描述、跳 /blog/[slug]、无结果显示「没有匹配的结果。」）。
- 改动：
  - `build.js`：blog 收集处补充 `category`(frontmatter，默认"博客")/`readingTime`(复用 `computeChapterMeta`)/`body`(纯文本 `stripForText`)；新增 `buildSearchIndex()` 输出 `public/search-index.json`（slug/title/description/tags/category/date/readingTime/body），在 `main()` 中 `buildAll` 后调用。
  - `templates/base.html`：nav-actions 加搜索按钮(⌘K kbd)；body 末尾加 search 弹窗 DOM（遮罩+毛玻璃卡片+输入框+结果+底部提示）；head 注入 `window.SITE_BASE_URL`。
  - `templates/home.html`：ghcal-card 加 `data-ghcal-palette="blue"` 与分段配色切换按钮（blue/green/grape）。
  - `static/css/style.css`：
    - 新增 `--font-sans`/`--font-display` 变量，body 与 h1–h6/.section-title/.logo 等统一 Apple 字体栈。
    - ghcal 加 `::after` 毛玻璃高光层 + `.ghcal-toolbar`/`.gh-pal-btn` 切换按钮样式；新增 green/grape 两套 5 级调色板（亮暗各一组，scoped 于 `.ghcal-card[data-ghcal-palette=...]`）。
    - 新增 search modal 全套暗色苹果毛玻璃样式（overlay 半透明黑+blur、modal spring 入场 `cubic-bezier(.34,1.56,.64,1)`、结果项布局、`mark` 高亮、底部提示）。
  - `static/js/main.js`：新增 `setupGhcalPalette()`（localStorage 持久化切换）与 `setupGlobalSearch()`（fetch 索引、⌘K/Ctrl+K 开、Esc/遮罩关、input 实时加权搜索、空查询取最近6篇、↑↓ 导航、↵ 打开、无结果提示）。
- 验证：`node --check` build.js/main.js 通过；`npm run build` exit 0，search-index.json 6 篇；用真实索引跑打分逻辑确认加权/排序/空查询正确；`SITE_BASE_URL` 注入为 ""（根部署，fetch 为 `/search-index.json`）。
- 提交：`735140c` feat: global ⌘K search, 3 heatmap palettes, Apple font & glass
- 推送：`git push origin main` **成功**（`416fba7..735140c`），CI 将部署。
- 预览文件（勿提交）：`github-activity-preview.html`（三配色）、`search-preview.html`（搜索弹窗）、`latex-pdf-demo.html`（LaTeX/PDF 演示）。

## Apple 级高帧动效 + 极致性能
- 诉求：类似 Apple 的高级高帧动效，并确保极致性能。
- 改动（commit `41be742`，已推送 `735140c..41be742`，CI 部署）：
  - 新增缓动令牌 `--ease-spring/--ease-out-expo/--ease-out-quint/--ease-in-out` + `--dur-*`，并将 `--transition` 的 `all` 改为具体属性（避免触发 layout/paint）。
  - hero 逐字动画去掉昂贵的 `filter:blur` 动画（改 translateY+scale+opacity spring）；`.char` 移除永久 will-change（避免逐字合成层）。
  - scroll reveal 弹簧化 + 入场时临时 will-change、transitionend 后 `classList.add('done')` 释放 GPU 层。
  - 合并 3 个独立 scroll 监听为单一 rAF 节流调度器（`onScrollFrame`/`scheduleScroll`），每帧只读一次布局。
  - 性能：`.post-content` 加 `content-visibility:auto` + `contain-intrinsic-size`；hero 头像 `fetchpriority=high decoding=async`；markdown 图片 `loading=lazy decoding=async`（`lazyImages()`）。
  - premium 微交互：按钮弹簧 hover + `:active` 缩放；卡片 hover 上浮 + 3D tilt 弹簧归位（JS 跟踪时 `transition:none` 防滞后）；页面淡入；全局 `prefers-reduced-motion` 兜底关闭动画/平滑滚动。
- 验证：`node --check` 通过、`npm run build` 零错误、public 产物确认含全部改动。

## 热力图回退为单一蓝色配色
- 用户诉求：热力图「只留蓝色配色 + 毛玻璃层」，去掉三配色切换。
- 改动（commit `1653e0d`，本地已提交，push 待网络恢复）：
  - `templates/home.html`：删除 `.ghcal-toolbar` 配色切换按钮组，移除卡片 `data-ghcal-palette="blue"` 属性。
  - `static/css/style.css`：删除 `.ghcal-toolbar`/`.gh-pal-btn` 切换器样式 + green/grape 两套 5 级调色板（含暗色），仅保留蓝色基色 `.gh-l0..4` 与毛玻璃 `::before`/`::after` 层。
  - `static/js/main.js`：删除 `setupGhcalPalette()` 调用与函数。
- 验证：`npm run build` exit 0；public 中 gh-pal-btn=0、green/grape=0、data-ghcal-palette=0；蓝色基色与玻璃层保留。

## 前端体检 + 设计一致性/无障碍/性能修复
- 诉求：作为前端/博客设计专家，调整不合理处并给高端设计意见。
- 已修（commit `dfb0cb9`，已推送 `41be742..dfb0cb9`，CI 部署）：
  1. **配色一致性**：全站散落的紫色 `#8e44ad`（hero 标题/打字机/阅读进度条/卡片流光边框）统一改为蓝青 `#5ac8fa`；`--glow-color-2` 由紫 `142,68,173` 改青 `90,200,250`。单一冷色系，更 Apple。
  2. **暗色图片变暗**：删除 `[data-theme="dark"] img{opacity:.85}` 与 `.md-content img{opacity:.9}`，头像/配图不再发灰。
  3. **性能**：移除 `.card:hover{will-change:transform}`（避免悬停卡片长期占合成层）。
  4. **无障碍**：新增 `:focus-visible` 主色描边环 + `:focus:not(:focus-visible)` 抑制鼠标态，键盘可达性补齐。
  5. **性能**：hero starfield canvas 增加 `pointer:fine && innerWidth>=1024` 门槛，低端移动端不跑全屏 canvas 动画。
- 待提（高端设计参考，已口头给出，未实现）：动效节奏统一调度、section 分隔线降噪、字号模块化比例、博客正文排版 measure/首字下沉、卡片流光边框改一次性淡入、footer 信息架构、亮暗对比度微调、焦点态 + 暗色图片处理策略。

## 本地误提交回退（仓库维护）
- 发现：本地 main 领先 origin/main 1 个提交 `066786c`（备注"1"），误将不该入库的内容提交：预览文件 `search-preview.html`/`latex-pdf-demo.html`、改动过的 `github-activity-preview.html`、以及工作记忆 `.workbuddy/memory/2026-08-13.md`。
- 处理：`git reset --mixed origin/main`（HEAD 回到 `dfb0cb9`，与 origin/main 对齐），文件保留本地磁盘、移出 git 跟踪/暂存，不被部署。
- 现状：本地 main 与 origin/main 一致（up to date）；工作区残留未跟踪的 `search-preview.html`/`latex-pdf-demo.html` 与未暂存的 `github-activity-preview.html`/`.workbuddy/memory/2026-08-13.md`，均不影响部署。
- 约定重申：根目录 `*-preview.html` 预览文件与 `.workbuddy/memory/` 工作记忆**勿提交**。

## Apple 风格动效系统（Motion System v2）
- 诉求：全站加 Apple 风动效，要「炫酷丝滑」。在已有动效底子上做系统化升级 + 高价值增量（commit `7e3638d`，已推送 `dfb0cb9..7e3638d`，CI 部署）。
- 改动（`static/css/style.css` / `static/js/main.js` / `templates/home.html`）：
  - CSS 令牌：新增 `--ease-spring-pop` / `--ease-in-quart` / `--dur-reveal` / `--blur-in` / `--stagger-step`。
  - 跨页 View Transitions：`@view-transition{navigation:auto}` + root 旧页淡出/新页淡入上滑（`vt-page-out/in`），reduced-motion 关闭。不支持的浏览器自动忽略。
  - 统一 Reveal 原语：`[data-reveal]` 支持 fade-up/fade/scale/zoom/slide-left/slide-right/blur；JS `setupScrollReveal` 升级为数据驱动（默认目标补 fade-up，stagger 沿用，入场后清 transitionDelay + 释放 will-change）。已确认无模板静态用 `.reveal`。
  - Hero 标题：逐字 spring → 遮罩上滑揭示（`.char-mask{overflow:hidden}` 包 `.char`，`char-mask-rise` 含 blur）；中文标题无 descender 裁剪风险。
  - 滚动视差：hero 背景包进 `.hero-bg`（`inset:-60px 0` 余量），`setupScrollScrub` 走中央 rAF 让背景随滚动下移 48px 制造纵深（hero `overflow:hidden` 裁边）。
  - 微交互：nav 下划线 width→transform scaleX（GPU 高帧）；按钮 sheen 光晕扫过；标签 chip 弹簧 hover；`.md-content a`/`.card-title-link` 改左生长下划线（去默认 underline）。
  - 模板：home.html hero 背景包 `.hero-bg`；ghcal-card `data-reveal="blur"`；5 个 section-subtitle `data-reveal="fade"`。
- 验证：`node --check` OK；`npm run build` 零错误（exit 0，54 页）；产物含 hero-bg/char-mask(115)/data-reveal(6)/view-transition；本地 8080 预览 HTTP 200。

## 布局规范化 + 动效增强（v3，全面改造）
- 诉求：按详细规格做「界面布局合理性 + Apple 动效系统」全面改造，先出审查清单再实现。
- 审查结论：已达标——1200 内容宽、section 100px 呼吸、卡片留白 35px、行高 1.7、首屏三要素居中、毛玻璃、暗色、动效 v2。待改 10 项——字重三层/字号标尺/触摸目标 44/间距令牌/断点 1024/页脚扁平/CLS/view-timeline/8px 网格/Apple 缓动。
- 实施（commit `cd47469`，已推送 `7e3638d..cd47469`，CI 部署）：
  - 设计令牌：8px 间距阶梯 `--space-*`、模块化字号 `--text-*`、三层字重 `--weight-regular/semibold/bold`、Apple 标准缓动 `--ease-apple`/`--ease-apple-out`、`--touch-min:44px`、玻璃分层 `--glass-blur-*`。
  - 布局修正：触摸目标统一 44px（theme-toggle/search-trigger/social/back-to-top）；stats-grid gap 20→24px；断点 992→1023px；section padding 用 `--space-12/10`；三层字重应用（标题 semibold、正文/副标题 regular）；hero 头像 `aspect-ratio:1/1` 防 CLS。
  - 页脚扁平化：base.html 改三栏（品牌/站点导航/社交）+底部版权，移动端堆叠居中。
  - 动效增强：CSS `view-timeline` 滚动驱动（`timeline-scope` + `view-timeline-name`，hero-grid 滚动淡出，`@supports` 渐进增强 + reduced-motion 关闭）。
- 验证：CSS 括号 520=520 平衡；node --check OK；build 零错误；产物含 footer-grid/view-timeline/新令牌；本地 8081 预览 200。
- 备注：8080 旧预览进程异常（502），改用 8081；localhost 校验需 `curl --noproxy '*'` 绕沙箱代理。

## 全面 Apple 风格改造 v4 (DESIGN SYSTEM UPGRADE)
按用户三部分规格（布局合理性 / Apple 动效 / Apple 设计原则）全面改造。

### 输出：布局审查清单
对照三部分需求逐项盘点 70+ 检查点，给出现状（✅已达标/⚠️部分/❌缺失）。

### 改造实施（追加覆盖层，不破坏既有逻辑）
**style.css 末尾追加 v4 增强层（593/593 花括号配平，3360 行）**：
1. **黄金比字号阶梯（1.618）**：重定义 `--text-xs..4xl` 为 0.764/0.875/1/1.236/1.618/2.058/2.618/3.236/4.236 rem；接入 h1-h4、.hero-text h1（clamp 流式）、.section-title、.page-header h1、.card-info h4。
2. **60/30/10 色彩语义**：新增 `--surface-60/30`、`--accent-10`，应用到 body、.section-alt、.btn。
3. **12 列栅格系统**：.grid + .col-span-12..1 + 断点 1024→8 列、767→4 列；卡片网格规范化：桌面 3 列 / 平板 2 列 / 移动 1 列。
4. **aspect-ratio CLS 防护**：.hero-image img 1/1、.card-img 16/9、.md-content img 16/9 + object-fit cover。
5. **hover 规格化**：.card translateY(-4px) + box-shadow，0.3s ease-apple。
6. **CSS view-timeline 滚动驱动补全**：`@supports(animation-timeline:view())` 下，body `timeline-scope: --vt-section`、每个 .section/page-header/content-page 独立 `view-timeline-name`；标题 `entry 0% contain 0%` 先行揭示、卡片错开 `entry 0% contain 40%`；**三列卡片左→中→右交错**（nth-child 3n+2/3n+3 的 animation-range 偏移 8%/16%）。新增 `@keyframes vt-title/vt-card`。
7. **首屏图文错开入场**：`@supports not (animation-timeline)` 下 hero-text 0.6s、hero-image 0.7s+0.2s delay。
8. **数字计数器标签同步淡入**：CSS 新增 .stat.is-counting/.is-counted 切换 stat-label opacity。

**main.js 增强**：
1. `setupScrollReveal`：检测 `CSS.supports('animation-timeline','view()')`，支持则跳过（CSS view-timeline 接管），JS 仅作降级兜底。
2. `setupCounters`：animateCount 加 done 回调；setupCounters 给 .stat 加 is-counting → is-counted 切换；reduce/无 IO 路径也加 is-counted。
3. `setupHeroTitle`：STEP 0.045 → **0.03**（首屏整体 0.8s 内完成规格）。
4. CSS `.hero-text h1 .char / .char-unit` 动画时长 0.85s → **0.65s** 配合 STEP。

### 验证
- 构建：`node build.js` exit 0，55 HTML。
- JS：`node --check main.js` 通过。
- CSS：593/593 花括号配平。
- 截图（headless Chrome，file:// 预览）：
  - 桌面首页：hero 标题 "你好，我是 王汉翰" 黄金比 4.236rem 清晰、头像 1:1 aspect-ratio、CTA 按钮可见。
  - 移动端首页：响应式正常，hero 单列堆叠。
  - **课程列表页：✅ 三列卡片栅格完美生效**，16:9 卡片图、黄金比标题、蓝色品牌色标签 chips。

### 文件变更
- `static/css/style.css`：+约 220 行（v4 增强层，3141→3360 行）
- `static/js/main.js`：setupScrollReveal +5 行 / animateCount+setupCounters 增强约 20 行 / setupHeroTitle STEP 调整 1 行
- 模板/base.html：**未改**

### 未改但用户提到可关注
- 毛玻璃材质分层应用（nav 用 medium、card 用 light、ghcal 用 strong）现有变量已定义但未统一应用；现有 `--glass-blur-*` 变量就绪，未来组件按规格选用即可。
- mobile 端 nav-links 在 390px 视口下疑似未完全隐藏（截图右上看到菜单项文字），这是改造前现状，不在本次范围。

## 导航栏显示问题修复 (NAVBAR FIX)
用户反馈"导航栏显示问题"。headless Chrome 截图 + computed style dump 定位两个独立问题：
1. **桌面对比度低**：`--nav-bg: rgba(255,255,255,0.72)` 在 hero aurora 浅蓝背景上文字难以辨认。
2. **移动端 nav-links 泄露**：原用 `right:-100%` 隐藏抽屉，headless/部分渲染环境下不可靠，导致"项目/博客"垂直外露。

### 修复（style.css）
- `--nav-bg` light: 0.72→**0.88**；`nav.scrolled` 0.72→**0.92**
- `--nav-bg` dark: 0.72→**0.88**；`nav.scrolled` dark 0.72→**0.92**
- 移动端 `@media(max-width:768px) .nav-links`：`right:-100%` 改为 `transform:translateX(100%)` + `visibility:hidden` + `pointer-events:none`；`.active` 时 `transform:translateX(0)+visibility:visible+pointer-events:auto`。过渡 `transform .4s` 同步。

### 验证
- `node --check` 无需（仅 CSS）；CSS 593/593 花括号配平
- `node build.js` exit 0，55 HTML
- computed style dump 确认：nav.bg=rgba(255,255,255,0.88)；移动端 nav-links visibility=hidden/transform=matrix(1,0,0,1,320,0)/pointerEvents=none ✓
- 截图：/tmp/wb_nav_final.png (1440)、/tmp/wb_nav_mobile_final.png (390) 供用户查看

## 导航栏显示问题第二轮修复 (NAVBAR FIX v2)
用户反馈"导航栏还是存在问题"。深入排查后发现：

### 诊断过程
1. 启用 swiftshader GPU 截图（去掉 --disable-gpu），覆盖 5 种状态（桌面初始/滚动态/暗色/平板/移动）
2. 发现即使启用 GPU，截图仍显示导航栏区域几乎空白
3. 注入 force-nav-visible（黑底黄字）强制染色后：**nav-links 垂直可见但 logo/nav-actions/hamburger 不可见**
4. 精确 DOM dump（computed style）揭示：
   - VW=756px 时（dump-dom 默认 viewport），所有元素存在且属性正确
   - nav-links 正确 hidden+translateX(320px)
   - logo visible, nav-actions visible, hamburger visible
   - **headless 截图与 dump 的 viewport 不一致导致误判**
5. 排查 z-index 冲突和 position:fixed 覆盖层——无异常
6. 确认暗色模式 --text=#f5f5f7（浅色），变量正确

### 结论
headless Chrome 渲染环境与真实浏览器差异过大，截图不可靠。转为**代码级防御性修复**。

### 本轮修复（style.css +8 行，3395 行，601/601 配平）
1. **backdrop-filter 实色 fallback**：`@supports not (backdrop-filter: blur(1px))` 下 nav 用 rgba(255,255,255,0.96) / dark rgba(28,28,30,0.96)
2. **文字可读性 text-shadow**：.logo 和 .nav-links a 加微妙文字阴影（light: 白色阴影, dark: 黑色阴影）
3. **移动端防泄露**：`@media(max-width:768px) nav { overflow: hidden }`
4. **保留上轮修复**：--nav-bg 0.88/0.92、transform:translateX(100%) 隐藏

### 验证
- CSS 601/601 配平 ✅ | JS --check 通过 ✅ | build.js exit 0 ✅
- 构建产物确认全部修复就位

## M4 全链路渲染验证 (23:43)

### 发现并修复的问题
1. **Light 模式导航栏对比度不足**: `--nav-bg` 从 0.88 提升到 0.95，新增 `border-bottom: 1px solid rgba(0,0,0,0.06)` 确保导航栏在浅色背景上可见
2. **移动端导航抽屉泄漏**: `transform:translateX(100%)` + `visibility:hidden` + `opacity:0` 在 headless Chrome 中仍渲染可见 → 新增 `clip-path:inset(100% 0 0 0)` 作为硬遮罩
3. **CDP 测试脚本 localStorage 泄漏**: 连续场景间 dark theme 残留导致 light 场景显示为 dark → 改为每次 clear LS 后 navigate，仅 dark 需要 reload

### 技术要点
- 使用 Node 22 全局 WebSocket 驱动 Chrome DevTools Protocol (CDP) 进行精确截图
- Chrome 启用 `--enable-unsafe-swiftshader` 确保 backdrop-filter 正常渲染
- 设备模拟: Emulation.setDeviceMetricsOverride 控制视口/DSF/mobile
- 7 个场景全覆盖: desktop × (light/dark) × (top/scrolled), mobile × (light/dark) × (closed/menu)
- `.render-check/cdp-shoot.cjs` 可复用的 CDP 渲染测试工具
